# vim: set ft=gitconfig:
[status]
	showUntrackedFiles = no
[hook "scripts"]
	event = post-commit
	event = post-checkout
	event = post-merge
	event = post-rewrite
	command = sh ~/.config/dotfiles/scripts.sh
